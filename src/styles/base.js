import { StyleSheet } from 'react-native'
import colors from './colors';

// Supply to Text
export default StyleSheet.create({
  screenTitle: {
    fontSize: 30,
    color: colors.white,
    fontWeight: '300',
  },
})